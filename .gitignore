node_modules
dist
.wrangler
*.local
.DS_Store
.vscode
*.tsbuildinfo

# Built card deck — regenerate with `npm run cards`.
dist-cards

# Built partner rate card — regenerate with `npm run rate-card`.
dist-partner

# Local secrets for `npm run dev` and `wrangler dev`. Never commit.
.dev.vars
.env
.env.*
shots
